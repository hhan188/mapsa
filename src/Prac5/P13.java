package Prac5;

public class P13 {
    public static boolean canPack(int bigPack, int smallPack, int goal) {
        if (bigPack < 0 || smallPack < 0 || goal < 0)
            return false;

        int bigWeight = bigPack * 5;
        return false;
    }
}
